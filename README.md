1. Install dependencies

   ```bash
   npm install
   ```

2. Start the app

   ```bash
   run
   npx expo start --clear --tunnel 
   or
   
   or
   npx expo start --dev-client

   to build again
   npx expo run:android
  
  

